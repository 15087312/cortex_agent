# Web 框架
# fastapi 锁定版本：0.141+ 的 include_router 改为惰性 _IncludedRouter（§54），
# >= 会导致 CI 装最新、本地停旧版的版本漂移（见 §55）。锁到已验证的 0.141.1。
fastapi==0.141.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic>=2.5.0

# 配置
PyYAML>=6.0
pydantic-settings>=2.1.0

# 安全/加密
cryptography>=42.0.0

# 异步支持
aiohttp>=3.9.1

# 数据库
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
redis>=5.0.1

# AI/ML
torch>=2.1.0
transformers>=4.35.0
jieba>=0.42.1
sentence-transformers>=2.2.2
accelerate>=0.25.0  # 模型加速
mlx-lm>=0.19.0  # Apple Silicon MLX 推理框架
mlx-vlm>=0.1.0  # Apple Silicon MLX 视觉模型（Qwen2-VL 等）
scikit-learn>=1.3.0  # TF-IDF 语义匹配
faiss-cpu>=1.7.4  # 向量索引

# 语音处理
SpeechRecognition>=3.10.0
gTTS>=2.5.0
openai-whisper>=20231117  # 本地语音识别 STT（随 torch 一起安装）
# 本地硬件依赖（pyaudio/pynput/pyautogui/pyserial）见 requirements-voice.txt ——
# 依赖系统库（portaudio 等），服务器/无头环境无法编译且无用

# 图像处理
Pillow>=10.1.0
opencv-python>=4.8.1.78

# 感知系统
mss>=10.0.0  # 跨平台屏幕捕获
watchdog>=6.0.0  # 文件变化监控
mcp>=1.0.0  # Model Context Protocol — 外部工具执行标准
rapidocr_onnxruntime>=1.2.0  # 轻量 OCR（ONNX 推理）
numpy>=1.24.0  # 数值计算（帧差/向量）
qwen-vl-utils>=0.0.10  # Qwen-VL 视觉模型工具

# OmniParser UI 结构化检测（可选，独立部署）
# 部署方式：git clone https://github.com/microsoft/OmniParser.git
#          cd OmniParser && pip install -r requirements.txt && python -m omnitool.omniparserserver.omniparserserver --port 8000
# 不部署时自动降级到 OCR，功能可用但精度较低
ultralytics>=8.1.0  # OmniParser 依赖（YOLO 目标检测）
supervision>=0.18.0  # OmniParser 依赖（检测结果处理）

# 工具库
python-dotenv>=1.0.0
loguru>=0.7.2
tenacity>=8.2.3
cachetools>=5.3.2

# 序列化
orjson>=3.9.10
ujson>=5.8.0

# 流式输出
sse-starlette>=2.0.0

# 监控与性能
prometheus-client>=0.19.0
psutil>=5.9.6

# HTTP 客户端
httpx>=0.25.2
requests>=2.31.0
crawl4ai>=0.2.0,<1.0.0

# 磁盘缓存（modules/database/disk_cache.py 可选依赖，装上有磁盘缓存能力）
diskcache>=5.6.0
