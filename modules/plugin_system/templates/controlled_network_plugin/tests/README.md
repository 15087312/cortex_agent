# Tests

Suggested future tests:

- Model actor does not see this tool by default.
- Expert actor can inspect it with approval.
- Path must start with `/`.
- Only the declared host is reachable through Gateway policy.
